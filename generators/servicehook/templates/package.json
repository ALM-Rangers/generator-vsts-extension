{
  "scripts": {
    "package": "tfx extension create  --rev-version",
    "gallery-publish": "tfx extension publish --rev-version",
    "clean": "rimraf ./*.vsix"
  },
  "devDependencies": {
    "tfx-cli": "^0.4.5"
  },
  "name": "packagesh",
  "private": true,
  "version": "0.0.1"
}